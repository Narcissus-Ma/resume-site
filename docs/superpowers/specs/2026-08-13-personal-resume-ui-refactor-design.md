# 个人在线简历 UI 重构设计

## 目标

将现有网站重构为可通过管理页面切换职业方向的个人在线简历：主页提供有辨识度的个人品牌与快速概览，简历详情页提供专业、可打印的完整履历。

## 边界

- 仅修改前端展示层代码及其测试：页面、展示组件、样式、静态视觉资源与文档。
- 保持现有路由、API 路径、HTTP 请求方法、返回数据类型与管理页数据编辑能力不变。
- 不修改 `cloudflare/`、Worker、KV、认证逻辑和任何服务端代码。
- 不新增后端字段；不调整任何页面现有的数据来源、字段映射、数据读取时机、回退逻辑、链接语义或管理页数据行为。

## 内容与配置规则

- “可配置职业方向”沿用现有模式：主页与简历各自由对应管理页选择活动资料。两者允许独立切换，不承诺跨页面原子同步；UI 不应假设二者职业方向必然一致。
- 姓名、头像、主页社交入口、联系信息、页脚和简历抬头均沿用当前组件的数据来源与展示条件；本次仅调整其布局、视觉样式和响应式呈现。
- 不增加前端品牌配置，不替换或清理现有链接，也不改变头像加载、占位或数据缺失时的现有行为。

## 体验模型

### 主页：个人品牌与摘要

- 首屏按“姓名 → 可配置职业定位 → 简介 → 查看简历”建立视觉优先级。
- 保留当前头像位；头像的加载、占位与未配置或加载失败时的回退行为完全沿用现有实现，仅调整其容器布局、尺寸与视觉样式。
- 用单一语义强调色串联字标、头像边框、链接、按钮与时间线，亮暗主题各有对应 token。
- 技能、经历、项目采用不同的阅读节奏：能力摘要以标签/证据呈现，经历为时间线，项目以可访问的案例卡展示；避免重复的满屏区块、居中标题和装饰横线。
- 所有链接的渲染条件、URL 读取与缺失值处理完全沿用现有实现，仅调整链接的视觉样式、焦点样式与响应式布局。

### 简历详情页：阅读与打印

- 使用单栏文档式信息架构：个人信息、职业摘要、能力、项目、工作经历、教育、链接。
- 屏幕上保留少量表面层级，打印时移除导航、管理入口、阴影、主题背景和非必要操作。
- 以 `@page`、`break-inside: avoid`、清晰的行距与正文颜色提供 A4 打印稳定性；长条目允许自然分页，不让标题或卡片孤立在页尾。
- 联系方式/所有者信息继续经由现有 PDF 模态框提供，不新增或改动服务端数据。

## 前端架构

- 在 `src/index.css` 集中维护品牌、表面、文本、边框、焦点、间距和打印 token。此次触及的组件不得新增固定色值，并将既有固定色值迁移到语义 token；Ant Design 覆盖与打印色也引用 token。组件专属结构样式保留在 CSS Modules 或组件局部样式中，不把全部样式堆入全局。
- 将主页展示组件按职责调整：品牌首屏、能力摘要、履历时间线、项目案例、联系与页脚各自独立。
- 将简历展示组件按文档段落职责调整，但不改变其 props、既有 `ResumeData` 字段读取或条件渲染规则。
- 保持 `useHome`、`useResume`、`publicCatalogApi`、`homeApi`、`resumeApi` 的职责与接口不变；UI 重构不改请求层。

## 无障碍与响应式

- `html` 与 `body` 使用 `overflow-x: clip`，在 320、375、414、768px 不出现横向滚动。
- 交互元素具备 hover、focus-visible、active、disabled 状态；动效只过渡明确属性，并提供 `prefers-reduced-motion` 降级。
- 键盘 Tab 可达所有既有交互，焦点始终可见；不改变既有链接与操作的业务行为。
- 保留现有图片与替代文本的数据绑定，仅为新增的纯装饰图形补充 `aria-hidden`。
- 标题允许长中英文与职业名称换行；链接与按钮在窄屏不换成两行可点击文本。

## 验收标准

1. 三种语言、明暗主题、公开路由 `/` 与 `/resume` 均可正常展示；受保护路由 `/home-manage`、`/resume-editor` 与 404 行为保持可用。
2. 前端请求地址、方法、载荷、页面数据来源、字段映射、回退逻辑与现有 API/管理状态测试一致；测试不得删除或弱化；`cloudflare/` 无改动。
3. 现有链接、头像及空数据行为保持不变；重构不得改变其业务语义。
4. 首页在 320/375/414/768/1280px 无水平溢出，核心简历入口始终可见。
5. 打印使用 A4 与固定页边距，隐藏导航、PDF 模态框、管理入口、主题切换和所有操作控件；内容区不使用满屏/固定高度，长项目可自然分页。以浏览器打印预览人工核验中英文长标题、长项目和跨页可读性。
6. 新增或更新前端回归测试仅覆盖视觉结构、响应式、打印选择器与既有关键交互的可达性，不改变既有数据源或业务语义。
7. `pnpm test`、`pnpm typecheck`、`pnpm lint` 与 `pnpm format:check` 通过。
